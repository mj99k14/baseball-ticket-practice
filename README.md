# Baseball Ticket Booking Practice

👉 [한국어 버전 보기](#한국어-버전)

---

## プロジェクト概要

バニラ JavaScript のみを使用した、野球チケット予約フローの練習用 Web アプリケーションです。
実際の予約サイトに近い UX を再現しながら、高齢者でも迷わず操作できるシンプルな設計を意識して構築しました。

ビルドステップは不要で、HTML / CSS / JS ファイルをそのままブラウザで開くだけで動作します。

デモ: https://baseballticket.netlify.app/html/home.html

---

## 技術スタック

| 分類 | 内容 |
|------|------|
| 言語 | HTML5, CSS3, Vanilla JavaScript (ES6+) |
| 状態管理 | `window.appState` + `sessionStorage` |
| ビルド | なし（ファイル直読み） |
| デプロイ | Netlify |
| Lint / Format | ESLint, Prettier |

---

## 主な機能

- 試合日程の選択（本日から 4 日間の LG 戦を自動生成）
- 座席エリアの選択（内野席 / 外野席）
- 座席の選択（A・B エリア 5×5 グリッド、最大 4 席）
- 価格確認（チケット合計 + 手数料 3,000 ウォン）
- 配送方法の選択（現地受取 / モバイルチケット / 宅配 +3,000 ウォン）
- カード決済 / 無通帳振込による支払い
- 予約完了画面（予約番号の自動生成・予約内容サマリー）

**ページ遷移フロー**

```
ホーム → 日付選択 → 座席選択 → エリア選択 → 価格確認 → 配送選択 → 決済 → 完了
```

---

## 工夫した点

### 1. カード決済 UX（高齢者を想定した設計）

このプロジェクトの最大の工夫点です。

一般的なカード決済では、ユーザーが実際のカード番号・有効期限・CVC を手入力します。
しかし高齢者にとってこの操作は煩雑であり、個人情報の入力に対する不安も大きいと考えました。
そのため、以下のアプローチを採用しました。

**ランダムカードの表示方式**

実際のカード情報を入力させるのではなく、システムがランダムに生成した練習用カード（番号・有効期限・CVC）を画面上のカードビジュアルに表示します。
ユーザーはその表示値を見ながら入力欄に転記するだけで決済を完了できます。
実際のカード情報を一切扱わないため、個人情報漏洩のリスクをゼロにしています。

**生年月日による本人確認**

カード情報の入力に加え、配送情報の登録時に入力した生年月日との一致確認を決済時に行います。
認知症や操作ミスによる誤操作を防ぐための補助的なバリデーションとして機能します。

**3 回失敗でカードロック**

入力値が 3 回連続で不一致の場合、決済ボタンをロックします。
繰り返しの入力ミスを抑止する安全装置として組み込んでいます。

**CVC 入力時のカードフリップアニメーション**

CVC 欄にフォーカスが当たると、カードが CSS 3D トランスフォームで裏返り、CVC の位置を視覚的に案内します。
実際にカードの裏面を確認する行為をそのまま UI で再現することで、操作の意味を直感的に伝えます。

---

### 2. 座席選択の設計

複雑な座席マップを避け、学習用として扱いやすいシンプルな構造を採用しました。

- 座席は **A エリア・B エリア** の 2 区画に分け、各エリアに 5×5 = 25 席を配置
- ページ読み込み時に約 20% の座席をランダムで選択不可（満席）状態にすることで、実際の予約サイトに近い状況を再現
- 選択できる座席は最大 4 席まで。上限に達した場合はアラートで通知
- 選択済み座席をクリックすると確認ダイアログを表示し、誤操作によるキャンセルを防止
- 選択した座席番号（A1、B3 など）はリアルタイムで画面に反映され、現在の選択状態を一目で確認できます

---

## 今後の改善点

- `sessionStorage` のキー名の統一（現状 `GanmeHome` などの誤字が残存）
- `SelectDelivery.js` における価格の二重計算バグの修正
- レスポンシブデザインへの対応
- 座席マップのより視覚的な表現（行・列ラベルの追加など）

---

---

## 한국어 버전

## 프로젝트 개요

바닐라 JavaScript만 사용한 야구 티켓 예매 플로우 연습용 웹 애플리케이션입니다.
실제 예매 사이트에 가까운 UX를 구현하면서, 노인 사용자도 쉽게 조작할 수 있는 단순한 설계를 목표로 제작했습니다.

빌드 단계 없이 HTML / CSS / JS 파일을 브라우저에서 바로 열면 동작합니다.

데모: https://baseballticket.netlify.app/html/home.html

---

## 기술 스택

| 분류 | 내용 |
|------|------|
| 언어 | HTML5, CSS3, Vanilla JavaScript (ES6+) |
| 상태 관리 | `window.appState` + `sessionStorage` |
| 빌드 | 없음 (파일 직접 서빙) |
| 배포 | Netlify |
| Lint / Format | ESLint, Prettier |

---

## 주요 기능

- 경기 일정 선택 (오늘 기준 4일간의 LG전 자동 생성)
- 좌석 구역 선택 (내야석 / 외야석)
- 좌석 선택 (A·B 구역 5×5 그리드, 최대 4석)
- 가격 확인 (티켓 총액 + 수수료 3,000원)
- 배송 방법 선택 (현장수령 / 모바일티켓 / 택배 +3,000원)
- 카드 결제 / 무통장 입금을 통한 결제
- 예매 완료 화면 (예약번호 자동 생성 및 예매 정보 요약)

**페이지 이동 흐름**

```
홈 → 날짜 선택 → 좌석 선택 → 구역 선택 → 가격 확인 → 배송 선택 → 결제 → 완료
```

---

## 공을 들인 부분

### 1. 카드 결제 UX (노인 사용자를 고려한 설계)

이 프로젝트에서 가장 중점적으로 고민한 부분입니다.

일반적인 카드 결제에서는 사용자가 실제 카드 번호, 유효기간, CVC를 직접 입력합니다.
하지만 노인 사용자에게 이 과정은 복잡하고, 개인정보를 입력하는 것에 대한 불안감도 크다고 판단했습니다.
이를 해결하기 위해 다음과 같은 방식을 채택했습니다.

**랜덤 카드 표시 방식**

실제 카드 정보를 입력받는 대신, 시스템이 무작위로 생성한 연습용 카드(번호, 유효기간, CVC)를 화면의 카드 비주얼에 표시합니다.
사용자는 화면에 보이는 값을 그대로 입력 칸에 옮겨 적는 것만으로 결제를 완료할 수 있습니다.
실제 카드 정보를 전혀 다루지 않기 때문에 개인정보 유출 위험이 없습니다.

**생년월일 본인 확인**

카드 정보 입력과 함께, 배송 정보 등록 시 입력한 생년월일과 일치하는지 결제 시점에 검증합니다.
조작 실수나 잘못된 접근을 방지하기 위한 보조적인 인증 단계입니다.

**3회 실패 시 카드 잠금**

입력값이 3회 연속으로 일치하지 않으면 결제 버튼을 잠급니다.
반복적인 입력 오류를 방지하는 안전장치로 동작합니다.

**CVC 입력 시 카드 뒤집기 애니메이션**

CVC 입력 칸에 포커스가 생기면 카드가 CSS 3D 트랜스폼으로 뒤집히며 CVC 위치를 시각적으로 안내합니다.
실제로 카드 뒷면을 확인하는 행동을 UI에서 그대로 재현해 조작의 의미를 직관적으로 전달합니다.

---

### 2. 좌석 선택 설계

복잡한 좌석 맵 대신, 학습용으로 다루기 쉬운 단순한 구조를 채택했습니다.

- 좌석은 **A 구역, B 구역** 2개 구역으로 나뉘며, 각 구역에 5×5 = 25석 배치
- 페이지 로딩 시 약 20%의 좌석을 랜덤으로 선택 불가(매진) 상태로 설정해 실제 예매 사이트와 유사한 상황을 재현
- 선택 가능한 좌석은 최대 4석. 한도 초과 시 알림으로 안내
- 이미 선택한 좌석 클릭 시 확인 다이얼로그를 표시해 실수로 인한 취소를 방지
- 선택한 좌석 번호(A1, B3 등)를 실시간으로 화면에 반영해 현재 선택 상태를 한눈에 확인 가능

---

## 개선 예정 사항

- `sessionStorage` 키 이름 통일 (현재 `GanmeHome` 등 오타 잔존)
- `SelectDelivery.js`의 가격 이중 계산 버그 수정
- 반응형 디자인 대응
- 좌석 맵의 시각적 개선 (행/열 라벨 추가 등)

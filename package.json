{
  "name": "@baseball/baseball-project",
  "version": "1.0.0",
  "description": "# baseball-ticket-practice",
  "main": "index.js",
  "scripts": {
    "lint": "npx eslint javascript/",
    "validate-html": "npx html-validator --file ./html/home.html && npx html-validator --file ./html/Completion.html && npx html-validator --file ./html/Seat-check-price.html && npx html-validator --file ./html/Select-date.html && npx html-validator --file ./html/Select-Delivery.html && npx html-validator --file ./html/Select-Seat-area.html && npx html-validator --file ./html/Select-Seat.html && npx html-validator --file ./html/Payment.html",
    "format:check": "npx prettier --check .",
    "format": "npx prettier --write ."
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "html-validator-cli": "^7.0.1",
    "prettier": "^3.8.1"
  }
}
